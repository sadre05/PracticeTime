#include<stdio.h>
int main()
{
	int n,r,sum=0;
	int tepm;
	scanf("%d",&n);
	temp=n;
	while(n>0)
	{
		r=n%10;
		sum=(aum*10)+r;
		n=n/10;
	}
	if(temp=sum)
	printf("the number is palindrome");
	else
	printf("no palindrome");
	return 0;
	
}
