#include<stdio.h>
int main()
{
    int a[10],i,sum=0,t;
       int n;
	   scanf("%d",&n);
    scanf("%d",&t);
    while(t--)
    {
    for(i=0;i<=n;i++)
    { scanf("%d",&a[6]);
        sum=sum+a[i];
        
    }printf("%d",sum);}
return 0;
}
